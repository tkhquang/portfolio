import React from "react";
import "./Loading.scss";

const Loading = () => (
  <div className="loading">
    <div className="loading-bar" />
    <div className="loading-bar" />
    <div className="loading-bar" />
    <div className="loading-bar" />
  </div>
);

export default Loading;
